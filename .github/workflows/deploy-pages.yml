name: Deploy GitHub Pages

on:
  push:
    branches:
      - main
    paths-ignore:
      - public/lectures.json
  workflow_dispatch:
  schedule:
    # 06:00 KST
    - cron: "0 21 * * *"

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: github-pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set Pages base path
        id: base
        shell: bash
        run: |
          owner="${GITHUB_REPOSITORY_OWNER,,}"
          repo="${GITHUB_REPOSITORY#*/}"
          if [[ "${repo,,}" == "${owner}.github.io" ]]; then
            echo "base_url=/" >> "$GITHUB_OUTPUT"
          else
            echo "base_url=/${repo}/" >> "$GITHUB_OUTPUT"
          fi

      - name: Set up uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Install Python
        run: uv python install 3.12

      - name: Install crawler dependencies
        working-directory: idec_crawler
        run: uv sync --locked

      - name: Generate lecture data
        id: crawler
        working-directory: idec_crawler
        timeout-minutes: 15
        continue-on-error: true
        env:
          IDEC_CONNECT_TIMEOUT_SEC: "5"
          IDEC_READ_TIMEOUT_SEC: "15"
        run: uv run python main.py

      - name: Verify lecture data
        shell: bash
        run: |
          if [[ "${{ steps.crawler.outcome }}" != "success" ]]; then
            echo "::warning::Crawler failed or timed out. Deploying the last committed lecture data."
          fi

          if [[ ! -s public/lectures.json ]]; then
            echo "::error::No lecture data is available."
            exit 1
          fi

      - name: Save latest successful lecture data
        if: steps.crawler.outcome == 'success'
        id: save-data
        continue-on-error: true
        shell: bash
        run: |
          if git diff --quiet -- public/lectures.json; then
            echo "Lecture data is unchanged."
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add public/lectures.json
          git commit -m "chore: update lecture data [skip ci]"
          git push origin HEAD:main

      - name: Warn when lecture data could not be saved
        if: steps.save-data.outcome == 'failure'
        shell: bash
        run: echo "::warning::The crawl succeeded, but lectures.json could not be committed. Check branch protection and Actions write permissions."

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: idec_tracker/package-lock.json

      - name: Install frontend dependencies
        working-directory: idec_tracker
        run: npm ci

      - name: Build static site
        working-directory: idec_tracker
        env:
          BASE_URL: ${{ steps.base.outputs.base_url }}
        run: npm run build

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: idec_tracker/build/client

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
